import { MessageBox, MessageContainer } from "./StyledMessage";

function Message() {

    <MessageContainer>
        <MessageBox >
            Messagem
        </MessageBox>
    </MessageContainer>
}

export default Message;
